services:
  booster-role:
    image: ghcr.io/mytheclipse/booster-role:latest
    container_name: booster-role
    restart: always
    env_file:
      - .env
    volumes:
      - booster-role-data:/app/data

volumes:
  booster-role-data:
